




// ______________________________________________________
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Author: Javed Kansi
// ______________________________________________________

package uk.co.planetbeyond.generated.comparator;

import java.util.Comparator;
import uk.co.planetbeyond.generated.WebUserBean;
import uk.co.planetbeyond.generated.WebUserManager;


/**
 * Comparator class is used to sort the WebUserBean objects.
 * @author sql2java
 */
public class WebUserComparator implements Comparator
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for WebUserComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new WebUserComparator(WebUserManager.ID_PASSWORD, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>WebUserManager.ID_PASSWORD
     *   <li>WebUserManager.ID_USERNAME
     * </ul>
     */
    public WebUserComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for WebUserComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new WebUserComparator(WebUserManager.ID_PASSWORD, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>WebUserManager.ID_PASSWORD
     *   <li>WebUserManager.ID_USERNAME
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public WebUserComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        WebUserBean b1 = (WebUserBean)pObj1;
        WebUserBean b2 = (WebUserBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case WebUserManager.ID_PASSWORD:
                if (b1.getPassword() == null && b2.getPassword() != null) {
                    iReturn = -1;
                } else if (b1.getPassword() == null && b2.getPassword() == null) {
                    iReturn = 0;
                } else if (b1.getPassword() != null && b2.getPassword() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getPassword().compareTo(b2.getPassword());
                }
                break;
            case WebUserManager.ID_USERNAME:
                if (b1.getUsername() == null && b2.getUsername() != null) {
                    iReturn = -1;
                } else if (b1.getUsername() == null && b2.getUsername() == null) {
                    iReturn = 0;
                } else if (b1.getUsername() != null && b2.getUsername() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getUsername().compareTo(b2.getUsername());
                }
                break;
            default:
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }}
